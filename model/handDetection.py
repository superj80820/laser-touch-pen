import sys, os

class handDetection(object):
    def __init__(self, image_path):
        self.image_path = image_path

    def detection(self):
        """
        請在這裡新增yolo function
        如果偵測到手 請回傳手的類型
        如果沒有偵測到 請回傳False
        """
        return True